<% @t = Tab.find 3 %>
<%= Assignment.find_by_sql("SELECT * FROM assignments WHERE tab_id = '#{@tab}' and customer_office_id IS NOT NULL;").count %>

<table class="table table-condensed table-striped">
<tr>
 <th> Tab Name </th>
 <th> Number of task </th>
</tr>

<% Tab.all.includes(:assignments).each do |t| %>
<tr>
	<% if t.assignments.present? %>
			<td><%= t.name %></td>
			<td><%= t.assignments.includes(:assignments).count %></td>
	<% else %>
		<td><%= t.name %></td>
		<td style="color:green"><%= t.assignments.includes(:assignments).count %></td>
 <% end %>
</tr>

<% end %>
<table>
